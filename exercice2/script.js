// firstPart est la première parti de ma variable.
let firstPart='Voici le début de ma phrase';
// lastPart est la seconde parti de ma variable.
let lastPart=' et ici la fin de ma phrase';
// alerte est une fonction
alert(firstPart + lastPart);